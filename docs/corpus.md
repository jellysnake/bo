# bo — Sample Corpus

A curated list of real URLs for manual QA and extraction quality review.
Not organised around failure modes (see `tests/test_urls.rs` for that) —
organised around content diversity and what people actually stash.

Run with `bo add <url>` and inspect the output.

## Wikipedia

| URL | Notes |
|---|---|
| https://en.wikipedia.org/wiki/Jaya_Sri_Maha_Bodhi | Tables, infobox, mixed content |
